# 仅为流程验证，不进入正式统计

该轮在绘图库准备期间启动，且完成期间给状态测试增加了 admission 断言，
因此其 environment.json 的初始源码快照不能完整描述后续状态/profile 运行。
全部原始数据保留，但本轮不作为论文测量结果，不与正式数据合并。

正式采样使用固定源码，在依赖准备及绘图结束后从头运行相同三场景、三种子、三进程。
正式目录：`../20260914-fixed-main-final/`。
